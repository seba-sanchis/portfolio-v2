export interface Contact {
  name: string;
  email: string;
  message: string;
}

export interface Project {
  title: string;
  description: string;
  image: string;
  tags: string[];
  repository: string;
  application: string;
}
